name: Sync Notion to Blog

on:
  workflow_dispatch: # 允許手動點擊執行
  schedule:
    - cron: '0 * * * *' # 每小時自動同步一次

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install @notionhq/client notion-to-md

      - name: Run sync script
        env:
          # 這兩行是把 GitHub Secrets 轉換成環境變數給 JS 用
          NOTION_TOKEN: ${{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}}
          NOTION_DATABASE_ID: ${{2cdc0300ab4380f38f18fe6683a15453}}
        run: node sync.js

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add posts/*.md
          git commit -m "Update posts from Notion" || echo "No changes to commit"
          git push
